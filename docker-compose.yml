version: '3.8'
services:
  dev_db:
    image: postgres:13
    restart: always
    ports:
      - 5432:5432
    env_file:
      - .env
    volumes:
      - db_data:/var/lib/postgresql/data   
    networks:
      - websmart

volumes:
  db_data:      
networks:
  websmart: